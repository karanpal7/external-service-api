import { logger } from '../config/logger';
import { config } from '../config/config';
import { ApiError } from '../utils/ApiError';

// Define interfaces for Fireblocks API responses
// Fireblocks SDK for development (replace with actual SDK in production)
class FireblocksSDK {

};
